<?xml version="1.0" encoding="UTF-8" ?>
<!-- mapper DTD 선언 -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- SQL Mapping -->
<mapper namespace="tales.akiba.seldesian.dao.UnionMapper">
	
	<sql id="unionColumns">
		union_no
		,union_name
		,union_lv
		,union_profile
		,union_lv_battle
		,union_lv_trade
		,union_lv_reputation
		,union_lv_life
		,union_expower
		,union_ticket
		,union_status
		,union_indate
		,union_editdate
		,union_deletedate
	</sql>
	
	
	<insert id="insertUnion" parameterType="tales.akiba.seldesian.entity.Union">
		INSERT INTO tbl_union (
		<include refid="unionColumns"/>
		) VALUES (
		NULL
		,#{union_name}
		,1
		,#{union_profile}
		,0
		,0
		,0
		,0
		,NULL
		,0
		,0
		,NOW()
		,NULL
		,NULL
		)
	</insert>
	
	<select id="selectUnionDetail" parameterType="long" resultType="tales.akiba.seldesian.entity.Union">
		SELECT
		<include refid="unionColumns"/>
		FROM
		tbl_union
		WHERE
		union_no = #{union_no}
	</select>
	
	<update id="updateUnion" parameterType="tales.akiba.seldesian.entity.Union">
		UPDATE tbl_union
		SET
			union_editdate = NOW()
			,union_name = #{union_name}
			,union_lv = #{union_lv}
			,union_profile = #{union_profile}
			,union_lv_battle = #{union_lv_battle}
			,union_lv_trade = #{union_lv_trade}
			,union_lv_reputation = #{union_lv_reputation}
			,union_lv_life = #{union_lv_life}
			,union_expower = #{union_expower}
			,union_ticket = #{union_ticket}
			,union_status = #{union_status}
		WHERE
			union_status = '1'
		AND
			union_no = #{union_no}
	</update>
	
	<update id="confirmUnion" parameterType="long">
		UPDATE tbl_union
		SET
			union_status = '1'
			,union_editdate = NOW()
		WHERE
			union_no = #{union_no}
	</update>
	
	<update id="deleteUnion" parameterType="long">
		UPDATE tbl_union
		SET
			union_status = '2'
			,union_deletedate = NOW()
		WHERE
			union_no = #{union_no}
	</update>
	
	<select id="selectUnionList" parameterType="tales.akiba.seldesian.entity.Union" resultType="hashMap">
		SELECT
		<include refid="unionColumns"/>
		FROM
			tbl_union
		WHERE
			union_status = '1'
		ORDER_BY
			union_no DESC
			,union_indate DESC
	</select>
	
	<select id="selectUnionTotalCount" parameterType="tales.akiba.seldesian.entity.Union" resultType="int">
		SELECT
			COUNT(*)
		FROM
			tbl_union
		WHERE
			union_status = '1'
	</select>
	
</mapper>